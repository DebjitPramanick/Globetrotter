import { LogoWrapper, LogoIcon, GlowEffect } from "./index.styled";

const Logo = () => {
  return (
    <LogoWrapper>
      <LogoIcon>
        <GlowEffect />
      </LogoIcon>
    </LogoWrapper>
  );
};

export default Logo;
